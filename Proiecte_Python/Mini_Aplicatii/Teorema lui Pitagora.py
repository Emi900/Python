# Teorema lui Pitagora 

# ip²= c² + c²
# BC² = AB² + AC²

import math


c1 = float(input("Introdu cateta 1 : "))
c2 = float(input("Introdu cateta 2 : "))

ip = math.sqrt(pow(c1, 2)+ pow(c2, 2))

print(f"Ipotenuza este {ip}")